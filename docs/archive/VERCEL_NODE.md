# Vercel Node Version

- Project targets Node 22.x.
- In Vercel → Project Settings → General → Node.js Version, set to `22.x`.
- Keep `.nvmrc` in repo aligned with the same major/minor.
- `package.json` engines currently: ">=22.0.0"; CI uses 22.x.
